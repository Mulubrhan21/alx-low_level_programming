#include "main.h"
/**
 * isupper - evaluate if letter is upper 
 *@c: number being tested
 * Return: Always 0.
 */
int _isupper( int c)
{
	if (c >= 'A' && C<='Z')
	{
		return (1);
	}
	else
	{
		return(0);
	}
}
