#include <stdio.h>
/**
* main -Entry point
*
* Return: always 0(Success/Correct)
*/
int main(void) 

{
int digit;

for (digit= 0; digit < 10; digit++)
{ 
print("%digit", digit);
printf("\n");
return(0);            
}
